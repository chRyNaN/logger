group 'com.chrynan'
version Versions.CORE

buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath ClassPath.KOTLIN
    }
}

apply plugin: 'kotlin'

repositories {
    mavenCentral()
}

dependencies {
    compile Libraries.KOTLIN
}